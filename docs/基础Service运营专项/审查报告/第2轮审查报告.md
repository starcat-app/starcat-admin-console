# 第 2 轮审查报告

> 审查时间：2026-08-27 18:52（Asia/Shanghai）  
> 审查范围：第 1 轮修复后的接口契约、BFF 请求模型、六服务运行链路与测试覆盖

## 检查项目

- 核对专项详细设计、`落地方案.md`、六服务 README 与真实路由；
- 检查 BFF fan-out 是否有重复请求、任意代理或密钥泄露；
- 使用备用端口同时启动六服务，验证业务统计和持久化指标；
- 复跑 Admin Console 完整质量门与 API Monitoring E2E。

## 发现的问题

| 编号 | 问题 | 严重程度 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R2-01 | Service 页面一个刷新周期会按每个统计卡片重复请求相同 `/internal/stats`；Sharing/Discovery 等服务会被重复计数并产生不必要负载 | 中 | 在单次 `loadService` 内按 `auth + path` 合并 Promise，同一响应供多个 `pick` 复用，并增加请求次数单测 | 已修复 |
| R2-02 | 第 1 轮后缺少六服务同时运行的真实进程验证证据 | 低 | 用临时 SQLite、备用端口 15001–15006 启动六服务，调用 health/ping/stats/metrics | 已修复 |

## 已通过检查

- 第 1 轮 5 个可修复问题均已关闭，依赖发布顺序作为明确前置条件保留；
- 六服务真实进程均返回 `requests=2`、`errors=0` 的持久化指标摘要；
- `pnpm check` 通过：41 个 Vitest、format、lint、typecheck、生产构建全部成功；
- 10 个 Playwright 测试通过，包含 Monitoring 曲线、路由排行与 Service 筛选；
- 六服务 README、专项详细设计和 `落地方案.md` 与实际路由一致；
- 临时运行数据已移入废纸篓，未访问生产数据或执行业务同步。

## 修复安排

完成 BFF 统计请求去重和单测后，提交独立中文 commit；随后进入第 3 轮最终审查，重点复核仓库状态、checklist、未完成项和最终交付报告。
