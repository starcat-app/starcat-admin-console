# 第 1 轮审查报告

> 审查时间：2026-08-27 18:41（Asia/Shanghai）  
> 审查范围：`starcat-api-kit`、六个基础 Service、`starcat-admin-console` 的文档、代码、测试与工程进度

## 检查项目

- 详细设计、README、checklist 和工程进度是否与当前实现一致；
- 指标采集是否只保留路由模板和聚合值，是否存在凭据或原始请求泄露；
- 六服务统计、数据视图和操作是否遵循已有鉴权边界；
- BFF 是否仅允许固定 Service、Range、Metric 与资源路径；
- Go 单测/`go vet`、Admin Console 单测/类型/lint/build/Playwright 是否通过。

## 发现的问题

| 编号 | 问题 | 严重程度 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R1-01 | 详细设计记录的 BFF `/api/observability/*` 与实际单一入口 `/api/services/observability` 不一致 | 中 | 以已实现的白名单路由为准修正文档 | 已修复 |
| R1-02 | 详细设计把 Service 详情写成五个独立页面，并声称使用 shadcn Chart 包装；实际为同页 Statistics/Data/Operations 分区并直接使用 Recharts | 中 | 修正文档信息架构和组件说明，不虚构未实现页面 | 已修复 |
| R1-03 | 保留策略描述为后台滚动聚合且宣称年度查询，实际写入时同步生成 minute/hour/day，当前查询范围上限为 180d | 中 | 修正聚合机制；把 day 标记为未来扩展预留 | 已修复 |
| R1-04 | 六服务 README 只记录了 metrics SQLite 配置，未完整列出新增运营与指标接口 | 中 | 分别补充只读运营接口、鉴权和隐私口径 | 已修复 |
| R1-05 | API Monitoring 缺少专门的浏览器 E2E 覆盖 | 低 | 增加曲线、路由排行和 Service 筛选测试 | 已修复 |
| R1-06 | `starcat-api-kit v0.3.0` 尚未发布，六服务离开本地 Go workspace 后暂时无法从远端解析该版本 | 中 | 文档明确发布顺序；本期不创建 tag、不 push、不加入长期本地 replace | 接受的交付前置条件 |

## 已通过检查

- 七个 Go 仓库 `go test ./...`、`go vet ./...` 与 `git diff --check` 全部通过；
- Admin Console 41 个单元测试、TypeScript、ESLint、生产构建和原有 9 个 Playwright 测试通过；
- 指标中间件使用 Go 路由模板，不持久化 Query、Header、Body、IP、User-Agent 或真实路径参数；
- BFF 单元测试确认 24 个指标上游请求均由固定路径生成，返回体不包含配置的测试密钥；
- Production 仍经 Gateway + `X-SC-Svc`，Test 仍直连六服务；Weekly/Discovery Admin Key 边界未改变；
- Starcat 主仓库 `docs/功能实现总览.md` 未修改。

## 修复安排

先补齐 E2E 与六服务 README，再同步专项详细设计、`落地方案.md`、README、checklist 和工程进度；所有修复完成后提交中文本地 commit，并进入第 2 轮审查。
