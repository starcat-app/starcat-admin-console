# 第 1 轮审查报告

- 审查时间：2026-08-27 02:15 CST
- 审查范围：Starcat Admin Console 数据平台、Trainer SQL Lab 与下载任务 JSON 契约、相关设计和使用文档
- 审查方式：文档对照、代码走查、Vitest、Ruff、mypy、pytest、Playwright、真实 ADC / PostgreSQL / BigQuery 运行验证

## 检查项目

### 文档审查

- 需求边界与实际一致：数据平台独立于六个业务 API，只在本机运行。
- SQL Lab 文档与代码一致：只读、`githubarchive`、dry run、hash、10 GiB、200 行 / 2 MiB。
- 下载任务文档与代码一致：固定 start / stop / restart，复用 screen、checkpoint 和磁盘保护。
- README、落地方案、Security、Privacy、Changelog、Trainer 使用说明和实施清单已同步。

### 代码审查

- BFF 使用 `shell: false`、固定 executable / argv、白名单环境和受限输出。
- SQL 只经 `0600` 临时文件传入 Trainer，并在 `finally` 中删除。
- PostgreSQL Catalog 不包含 SQL 正文和结果行字段。
- 下载状态只查询一次共享 Project quota，并有 30 秒快照。

### 测试审查

- Admin Console：Prettier、ESLint、TypeScript、34 个 Vitest 和生产构建通过。
- Playwright：7 个场景全部通过，包含 SQL dry run → Execute → 结果表完整流程。
- Trainer：Ruff、mypy、99 个 pytest 通过，覆盖率 87%。
- 真实链路：两类下载任务状态读取成功；`SELECT 1 AS ok` dry run / query 成功，扫描与计费均为 0 B。

### 工程进度审查

- Web、BFF、Trainer、PostgreSQL、文档和真实运行链路均已完成。
- `docs/功能实现总览.md` 未修改，符合主仓库未授权禁止写入约束。
- 所有功能提交均为本地中文 commit，未 push。

## 发现的问题

| 编号 | 问题 | 严重程度 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R1-01 | PostgreSQL 首次初始化失败后，Runtime 缓存 rejected Promise，数据库恢复后仍不能自动重试 | 中 | 初始化失败时清除本次 Promise，并增加失败后重试测试 | 已修复 |
| R1-02 | BFF 重启会丢失内存查询结果，但历史成功 Job 的 `resultAvailable` 仍可能保留为 true | 中 | Catalog 初始化时统一清除瞬时结果标识，并增加成功 Job 重启测试 | 已修复 |
| R1-03 | Trainer `make check` 依赖全局 `uv`，当前 shell 未安装到 PATH | 低（环境） | 使用项目 `.venv` 执行同等 Ruff / mypy / pytest 门禁；使用指南继续要求先安装/执行 uv | 已规避，非代码缺陷 |

## 修复计划

1. 修复 Runtime 初始化重试语义。
2. 修复 PostgreSQL 与内存 Catalog 的重启瞬时结果状态。
3. 新增回归测试并重新运行 Admin Console 全量门禁。
4. 生成第 2 轮审查报告，继续核对文档、代码、测试和工程进度。

## 修复结果

- Runtime 初始化失败后会清除失败 Promise，下一次请求可在 PostgreSQL 恢复后重试。
- PostgreSQL 与内存 Catalog 启动恢复都会清除全部瞬时结果标识，同时保持历史成功状态。
- 新增 2 个回归测试；Admin Console 36 个 Vitest、生产构建和 7 个 Playwright 场景全部通过。
